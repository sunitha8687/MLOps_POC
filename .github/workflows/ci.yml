name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: checkout mlops repository
      uses: actions/checkout@v1
    - name: Run python script
      run: echo model_code_changes.py
    - name: Test with pytest
      run: |
        pytest